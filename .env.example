VITE_TG_BOT_TOKEN=""
VITE_TG_CHAT_ID=""
# Used only by Vite dev server / preview middleware (not exposed in the RSVP fetch URL).
# Optional: absolute URL of your production RSVP endpoint (Netlify function, etc.).
# VITE_RSVP_API_URL="https://your-site.netlify.app/.netlify/functions/rsvp"
